import TodosComponent from "../components/todos/TodosComponent";

const TodosPage = () => {
    return (
        <div>
            <TodosComponent/>
        </div>
    );
}

export default TodosPage;
